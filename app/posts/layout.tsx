export const metadata = {
  title: " Feed | Next.js 13 Demo App. Attempt to build social app.",
};

export default function SubscribeLayout({
  children,
}: {
  children: React.ReactNode;
  }) {
  return <section>{children}</section>;
}
